_schema-version: "3.1"
ID: Product
description: A simple CAP project.
version: 1.0.0
modules:
- name: Product-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: Product-auth
  - name: Product-destination
  - name: postgres
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
  build-parameters:
    builder: npm
- name: Product
  type: approuter.nodejs
  path: app/
  requires:
  - name: srv-api
    group: destinations
    properties:
      forwardAuthToken: true
      name: srv-api
      url: ~{srv-url}
  - name: Product-auth
  - name: Product-destination
  parameters:
    disk-quota: 512M
    keep-existing-routes: true
    memory: 512M
- name: Product-postgres-deployer
  type: nodejs
  path: gen/pg
  requires:
  - name: postgres
  parameters:
    buildpack: nodejs_buildpack
    no-route: true
    no-start: true
    tasks:
    - command: npm start
      name: deploy-to-postgresql
- name: Product-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: Product-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - comsatinfotechproductproduct.zip
      name: comsatinfotechproductproduct
      target-path: resources/
- name: comsatinfotechproductproduct
  type: html5
  path: app/product
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: Product-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      tenant-mode: dedicated
      xsappname: Product-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
- name: Product-destination
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
    service: destination
    service-plan: lite
- name: postgres
  type: org.cloudfoundry.managed-service
  parameters:
    service: postgresql-db
    service-plan: trial
- name: Product-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: Product-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npx cds build --production
